import 'package:flutter/material.dart';

class DailyEvaluationPage extends StatelessWidget {

  const DailyEvaluationPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Avaliação Funcional'),),
           body: Container(),
       );
  }
}