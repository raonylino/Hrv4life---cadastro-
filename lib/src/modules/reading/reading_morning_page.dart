import 'package:flutter/material.dart';

class ReadingMorningPage extends StatelessWidget {

  const ReadingMorningPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Medida Matinal'),),
           body: Container(),
       );
  }
}